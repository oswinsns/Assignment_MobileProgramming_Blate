<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background Image -->
    <item>
        <bitmap
            android:src="@drawable/happycouple"
            android:gravity="fill"
            android:alpha="0.35" />
    </item>

    <!-- Mask shape (inset to correct height) -->
    <item
        android:drawable="@drawable/half_circle"
        android:top="0dp"
        android:bottom="0dp" />
</layer-list>
